package io.dataframe.expression;

/**
 * Represents an immutable expression in the JavaDataFrame query model.
 *
 * <p>An Expression describes a computation or condition that can later be
 * evaluated by the execution engine. Expressions are immutable and form
 * the building blocks of query operations such as filtering, sorting,
 * grouping, and aggregation.
 *
 * @since 1.0.0
 * @author Anirban Chatterjee
 */
public interface Expression {
}